buildscript {
    dependencies {
        classpath 'com.google.gms:google-services:4.3.15' // Replace with the latest version
    }
}

plugins {
    id "com.android.application"
    // id 'com.google.gms.google-services'
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
    
}

// dependencies {
//     implementation platform('com.google.firebase:firebase-bom:33.6.0')
//     implementation 'com.google.android.gms:play-services-base:21.5.0'
// }

// dependencies {
//     // Ensure that this dependency is declared
//     implementation 'com.google.android.gms:play-services-base:21.5.0'
//     // Other Firebase dependencies
//     implementation 'com.google.firebase:firebase-firestore:25.1.1'
//     implementation 'com.google.firebase:firebase-auth:23.1.0'
// }

android {
    signingConfigs {
        release {
            keyAlias 'my-key-alias'
            keyPassword 'iluv$56&'
            storeFile file('C:/Users/jilln/my-release-key.jks')
            storePassword 'iluv$56&'
        }
    } 

    namespace = "com.example.cc206_aac_app"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.cc206_aac_app"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false // For debugging issues, disable code shrinking
            shrinkResources false
        }
    }
}

flutter {
    source = "../.."
}

apply plugin: 'com.android.application'
apply plugin: 'com.google.gms.google-services'